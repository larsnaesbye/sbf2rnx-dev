//! Here we define unit tests and other tests to be run
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() { // TODO: replace with real tests
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}